        // Click on "Advanced" button
        WebElement advancedButton = driver.findElement(By.xpath("//button[contains(text(),'Advanced')]"));
        advancedButton.click();
        
        // Click on "Proceed to [URL] (unsafe)"
        WebElement proceedLink = driver.findElement(By.xpath("//a[contains(text(),'Proceed to')]"));
        proceedLink.click();
        
        // If a certificate selection appears, choose the first one
        try {
            WebElement certificateOption = driver.findElement(By.xpath("(//td[@class='subject'])[1]"));
            certificateOption.click();
            certificateOption.sendKeys(Keys.ENTER);
        } catch (Exception e) {
            System.out.println("No certificate selection required.");
        }
