import java.io.*;
import java.util.*;
import java.util.jar.*;

public class JarExtractor {

    public static Map<String, List<Object>> extractJarContents(String jarFilePath) {
        Map<String, List<Object>> folderToFileDataMap = new HashMap<>();

        try (JarFile jarFile = new JarFile(jarFilePath)) {
            Enumeration<JarEntry> entries = jarFile.entries();

            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();

                if (entry.isDirectory()) {
                    folderToFileDataMap.put(entry.getName(), new ArrayList<>());
                } else {
                    String folderName = entry.getName().substring(0, entry.getName().lastIndexOf('/') + 1);
                    List<Object> fileDataList = folderToFileDataMap.computeIfAbsent(folderName, k -> new ArrayList<>());

                    try (InputStream inputStream = jarFile.getInputStream(entry)) {
                        Object fileData = readInputStream(inputStream);
                        fileDataList.add(fileData);
                    }
                }
            }
        } catch (IOException e) {
            e.printStackTrace();
        }

        return folderToFileDataMap;
    }

    private static Object readInputStream(InputStream inputStream) throws IOException {
        StringBuilder fileData = new StringBuilder();
        BufferedReader reader = new BufferedReader(new InputStreamReader(inputStream));
        String line;
        while ((line = reader.readLine()) != null) {
            fileData.append(line).append("\n");
        }
        return fileData.toString();
    }

    public static void main(String[] args) {
        String jarFilePath = "src/main/resources/yourfile.jar";
        Map<String, List<Object>> folderToFileDataMap = extractJarContents(jarFilePath);

        // Example of how to print the contents of the map
        folderToFileDataMap.forEach((folder, files) -> {
            System.out.println("Folder: " + folder);
            for (Object fileData : files) {
                System.out.println("File Data: " + fileData);
            }
        });
    }
}
