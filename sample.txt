index="aws_ead_prod" sourcetype="*" NOT sourcetype="aws:/aws/lambda/lambda-PGP-synthetic-lambda*"
| rex "accountNumber\":\"(?<accountNumber>[^\"]+)\""
| rex "userId\":\"(?<userId>[^\"]+)\""
| rex "appid\":\"(?<appid>[^\"]+)\""
| stats count as login_count by appid, userId
| eval login_status = if(login_count > 1, "2 or more", "1")
| table _time, appid, userId, login_count, login_status
