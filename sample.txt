 ImageReadParam param = reader.getDefaultReadParam();
                            param.setSourceSubsampling(1, 1, 0, 0);
                            BufferedImage bufferedImage = reader.read(p, param);
                            
                            if (bufferedImage == null) {
                                LOG.warn("Skipping null image at page {}", p);
                                continue;
                            }
